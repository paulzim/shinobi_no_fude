# Optional LLM backend settings.
# Needed for non-deterministic fallback answers.
MODEL=llama3.2:3b
OPENAI_BASE_URL=http://127.0.0.1:11434/v1
OPENAI_API_KEY=
OPENROUTER_API_BASE=
OPENROUTER_API_KEY=
LM_STUDIO_BASE_URL=

# Optional index overrides.
# INDEX_DIR=./index
# CONFIG_PATH=./index/config.json
# META_PATH=./index/meta.pkl
# INDEX_PATH=./index/index.faiss

# Optional offline flags when the embedding model is already cached locally.
# HF_HUB_OFFLINE=1
# TRANSFORMERS_OFFLINE=1
